# Retailys Coding Test README

The script is relatively short, only uses functions from built-in libraries, and prints the functions on the standard output. 

## How to run:

In the terminal, run: 

`python3 soltes.py`